<%= simple_form_for [@user, @agenda], :html => {:class => 'form-horizontal'} do |f| %>
    <%= f.input :name %>
    <%= f.input :description %>
    <br/>
    <%= f.button :submit, :class => "btn" %>
<% end %>
