<%= simple_form_for [@user, @agenda, @item], :html => {:class => 'form-horizontal'} do |f| %>
  <%= f.input :topic %>
  <br/>
  <%= f.button :submit, :class => "btn "  %>
<% end %>
<%= link_to 'Back', :back %>
